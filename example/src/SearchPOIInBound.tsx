import React from 'react';
import {View, Text} from 'react-native';
import MapxusSdk from '@mapxus/react-native-mapxus-sdk';

export default function SearchPOIInBound() {
	return (
		<View style={{flex: 1}}>
			<Text>search POI in bound</Text>
		</View>
	)
}
